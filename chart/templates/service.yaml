apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-architecture-example.fullname" . }}
  labels:
    {{- include "python-architecture-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "python-architecture-example.selectorLabels" . | nindent 4 }}
